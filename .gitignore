# Environment variables - NEVER commit
.env
.env.local
.env.production

# Hardhat / Foundry
node_modules/
cache/
out/
artifacts/
typechain-types/

# Coverage
coverage/
coverage.json

# Claude Code internals
.claude/
CLAUDE.md

# MCP config (may contain private keys in env)
.mcp.json

# Hardhat ignition deployments (contain addresses, not secrets, but env-specific)
ignition/deployments/

# MCP server build artifacts
mcp-server/node_modules/
mcp-server/dist/

# Credentials (defensive)
*.key
*.pem
.env.*

# OS
.DS_Store
Thumbs.db
